# imapflow pins nodemailer@^9, while next-auth only lists nodemailer as an
# optional peer at ^7 (for its own unused EmailProvider) — a real
# ERESOLVE conflict, not a version we can bump our way out of. Needed for
# both local installs and Vercel's build (which runs plain `npm install`).
legacy-peer-deps=true
